<UserControl x:Class="CustomGridControl.Views.ComparisonGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CustomGridControl"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1200">
    <UserControl.Resources>
        <local:CellStateToBackgroundConverter x:Key="CellStateToBackgroundConverter"/>
        <local:CellStateToVisibilityConverter x:Key="CellStateToVisibilityConverter"/>
        
        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="Width" Value="18"/>
            <Setter Property="Height" Value="18"/>
            <Setter Property="Margin" Value="2,0,0,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="9"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="#F5F5F5" BorderBrush="#CCCCCC" BorderThickness="0,0,0,1" Padding="10">
            <StackPanel Orientation="Horizontal">
                <Button Content="Load Data" Click="LoadButton_Click" Padding="10,5" Margin="0,0,10,0"/>
                <Button Content="Save Changes" Click="SaveButton_Click" Padding="10,5" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Margin="20,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- Grid -->
        <telerik:RadGridView x:Name="ComparisonGrid" 
                             Grid.Row="1"
                             ItemsSource="{Binding ComparisonRecords}"
                             AutoGenerateColumns="False"
                             CanUserFreezeColumns="True"
                             CanUserReorderColumns="True"
                             CanUserResizeColumns="True"
                             ShowGroupPanel="False"
                             IsReadOnly="True"
                             RowIndicatorVisibility="Collapsed">
        </telerik:RadGridView>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#F5F5F5" BorderBrush="#CCCCCC" BorderThickness="0,1,0,0" Padding="10,5">
            <TextBlock>
                <Run Text="Records: "/>
                <Run Text="{Binding ComparisonRecords.Count, Mode=OneWay}"/>
                <Run Text=" | Fields: "/>
                <Run Text="{Binding FieldNames.Count, Mode=OneWay}"/>
            </TextBlock>
        </Border>
    </Grid>
</UserControl>
